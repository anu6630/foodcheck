# Supabase Connection URLs
# The cluster endpoint is fetched from Railway variables.
# You must create a database named "foodcheck" inside this cluster (via SQL client or Supabase dashboard) before connecting.

# Prisma / Web client URL (usually with pooling / port 5432 or 6543)
DATABASE_URL="postgresql://postgres.mhnreqfbohczronjprzs:<PASSWORD>@aws-1-ap-northeast-2.pooler.supabase.com:5432/foodcheck"

# Direct connection URL (used for migrations, direct pooler bypass if available)
DIRECT_URL="postgresql://postgres.mhnreqfbohczronjprzs:<PASSWORD>@aws-1-ap-northeast-2.pooler.supabase.com:5432/foodcheck"

# Gemini Vision API Key (for product/OCR intent analysis)
GEMINI_API_KEY=""
